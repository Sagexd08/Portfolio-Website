import React from 'react';
import { motion } from 'framer-motion';
import AIShowcase from './AIShowcase';

// This is a wrapper component that simply renders the AIShowcase component
// It's used to maintain compatibility with the existing code structure
const Showcase: React.FC = () => {
  return <AIShowcase />;
};

export default Showcase;
